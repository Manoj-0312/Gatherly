<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/eventsAttendedText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Events Attended: "
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/eventsOrganizedText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Events Organized: "
        android:textSize="16sp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/totalTicketsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total Tickets Purchased: "
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Future settings -->
    <Button
        android:id="@+id/notificationPreferencesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Notification Preferences (Coming Soon)"
        android:enabled="false"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/themeSettingsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Theme Settings (Coming Soon)"
        android:enabled="false" />
</LinearLayout>
